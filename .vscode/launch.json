{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run AirCode Extension",
      "type": "extensionDevelopmentHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "npm: build"
    }
  ]
}